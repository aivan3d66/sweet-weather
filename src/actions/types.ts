export type CoordinatesType = {
  lat: number;
  lon: number;
}

export type CurrentWeatherDataType = {
  date: string;
  temp: number;
  weatherIcon: string;
  weatherMain: string;
  wind: number;
  code: number;
}
